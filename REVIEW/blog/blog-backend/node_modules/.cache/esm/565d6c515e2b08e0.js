let jwt;_5e2‍.x([["default",()=>_5e2‍.o]]);_5e2‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);

const jwtMiddleware = (ctx, next) => {
  const token = ctx.cookies.get('access_token');
  if (!token) return next();
  try {
    const decoded = jwt.verify(token, process.env.JWT_SECRET);
    ctx.state.user = {
      _id: decoded._id,
      username: decoded.username,
    };
    _5e2‍.g.console.log(decoded);
    return next();
  } catch (e) {
    return next();
  }
};

_5e2‍.d(jwtMiddleware);
